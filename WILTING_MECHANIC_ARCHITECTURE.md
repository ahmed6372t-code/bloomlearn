# Wilting Mechanic - Architecture & Flow Diagrams

## System Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    USER COMPLETES STAGE                       │
│                   (Remember/Understand/Apply)                 │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │ completeStage() called│
              │  - Calculate accuracy │
              │  - Track maxCombo     │
              └──────────┬───────────┘
                         │
              ┌──────────┴──────────┐
              │                     │
          Did UNLOCK?              No UNLOCK
          (Accuracy >= 80%     (accuracy < 80% or
           && Combo >= 3)       combo < 3)
              │                     │
              ▼                     ▼
         ┌─────────┐          ┌─────────────┐
         │ SUCCESS │          │ FAILED GATE │
         │ STAGE   │          │ (No XP)     │
         └────┬────┘          └──────┬──────┘
              │                      │
              │ NEW BEHAVIOR:         │
              └─► lastPlayedTS        │
                  = Date.now()        │ (don't update timestamp)
              │                      │
              ▼                      ▼
        ┌────────────────┐      ┌──────────────┐
        │ Update in      │      │ Update in    │
        │ Firestore &    │      │ Firestore &  │
        │ AsyncStorage   │      │ AsyncStorage │
        │ with FRESH     │      │ with STALE   │
        │ timestamp      │      │ timestamp    │
        └────────┬───────┘      └──────┬───────┘
                 │                     │
                 └──────────┬──────────┘
                            │
                            ▼
              ┌─────────────────────────┐
              │  player reopens app     │
              │      (next day/week)    │
              └────────────┬────────────┘
                           │
                           ▼
              ┌─────────────────────────┐
              │ calculateFreshness()    │
              │ reads lastPlayedTS      │
              │ computes elapsed days   │
              │ returns % (0-100)       │
              └────────────┬────────────┘
                           │
              ┌────────────┴──────────────┐
              │                           │
          freshness === 100%         freshness < 100%
          (0-2 days)                 (> 2 days)
              │                           │
              ▼                           ▼
        ┌──────────────┐         ┌─────────────────┐
        │ StageCard    │         │ StageCard       │
        │ renders:     │         │ renders:        │
        │ - Green ✓    │         │ - Wilting UI    │
        │ - Stats      │         │ - Warning badge │
        │ - Normal     │         │ - Reduced       │
        └──────────────┘         │   opacity       │
                                 └─────────────────┘
```

---

## Data Flow: Completion to Display

```
┌─────────────────────────────────────────────────────────────────┐
│                PLAYER COMPLETES "REMEMBER" STAGE                 │
│                   Game component finishes                        │
└──────────────────────┬────────────────────────────────────────────┘
                       │
                       ▼
         completeStage(materialId, "remember", accuracy=0.92, maxCombo=5)
                       │
       ┌───────────────┼───────────────┐
       │               │               │
   Validate    Track Results  Award XP
   timestamp               │
       │                   ▼
       │   ┌─────────────────────────┐
       │   │ Check Mastery Gate:     │
       │   │ 0.92 >= 0.8? YES ✓     │
       │   │ 5 >= 3? YES ✓          │
       │   │ UNLOCK = true           │
       │   └────────────┬────────────┘
       │                │
       │                ▼
       │   ┌──────────────────────────┐
       │   │ IF unlocked = true:      │
       │   │ lastPlayedTimestamp      │
       │   │ = Date.now()             │
       │   │ (1708390000000)          │
       │   └────────────┬─────────────┘
       │                │
       │                ▼
       └──────────────────────────────────┐
                                          │
                                          ▼
                        ┌─────────────────────────────┐
                        │ UPDATE FIRESTORE            │
                        │ materials/{id}/stageResults │
                        │ {                           │
                        │   "remember": {             │
                        │     accuracy: 0.92,         │
                        │     maxCombo: 5,            │
                        │     attempts: 1,            │
                        │     timestamp: 1708390...   │
                        │     lastPlayedTS:1708390... │
                        │   }                         │
                        │ }                           │
                        └─────────────┬───────────────┘
                                      │
              ┌───────────────────────┼──────────────────────────┐
              │                       │                          │
        User logs in        [TIME PASSES]                  [Same Session]
        tomorrow            (3 days later)                (immediate)
              │                       │                          │
              ▼                       ▼                          ▼
         ┌─────────┐        ┌──────────────┐          ┌──────────────┐
         │ Load    │        │ Load from    │          │ Home screen  │
         │ from    │        │ Firestore    │          │ renders      │
         │ Firebase│        └──────┬───────┘          │ MaterialSection
         └────┬────┘               │                  └──────┬───────┘
              │                    │                         │
              ▼                    ▼                         ▼
    ┌──────────────────┐   ┌─────────────────┐    ┌─────────────────┐
    │ Cache in         │   │ lastPlayedTS:   │    │ For each stage  │
    │ AsyncStorage     │   │ 1708390... (3d) │    │ call:           │
    │                  │   │ Hydrate React   │    │ freshness =     │
    └────────┬─────────┘   └────────┬────────┘    │ calculateFresh..│
             │                      │              └────────┬────────┘
             │                      │                       │
             └──────────────────────┴───────────────────────┤
                                                             │
                                                             ▼
                                    ┌──────────────────────────────┐
                                    │ calculateFreshness(1708390..│
                                    │ elapsedDays = 3              │
                                    │ return 75% (DROOPING)        │
                                    └────────────┬─────────────────┘
                                                 │
                                                 ▼
                                    ┌──────────────────────────────┐
                                    │ StageCard component gets:    │
                                    │ freshness = 75               │
                                    │ isCompleted = true           │
                                    │ isDrooping = true            │
                                    └────────────┬─────────────────┘
                                                 │
                                                 ▼
                                    ┌──────────────────────────────┐
                                    │ Apply style:                 │
                                    │ stageCardDrooping {          │
                                    │   bg: #FFF9E6 (light gold)  │
                                    │   border: #FFD54F (gold)    │
                                    │ }                            │
                                    │ Show text:                   │
                                    │ 📉 Fading (review soon)     │
                                    └────────────┬─────────────────┘
                                                 │
                                                 ▼
                                    ┌──────────────────────────────┐
                                    │ HOME SCREEN SHOWS:           │
                                    │ 🌾 The Seed Library          │
                                    │ Gold border, slightly dim    │
                                    │ 92% Accuracy · x5 Combo     │
                                    │ 📉 Fading (review soon)     │
                                    └──────────────────────────────┘
```

---

## Freshness Timeline State Machine

```
                     NEVER PLAYED
                          │
                          ▼
                    ┌──────────────┐
                    │ freshness=100│
                    │  (Fresh)     │
                    └────────┬─────┘
                             │
                    [Player completes]
                             │
                    ┌────────▼─────────┐
                    │ lastPlayedTS set │
                    │ to Date.now()    │
                    └────────┬─────────┘
                             │
           ┌─────────────────┼──────────────────┐
           │                 │                  │
      [Day 0-2]         [Day 3-6]          [Day 7-13]
           │                 │                  │
           ▼                 ▼                  ▼
    ┌───────────────┐ ┌──────────────┐ ┌──────────────┐
    │ FRESH (100%)  │ │ DROOPING(75%)│ │ WILTING(50%) │
    │               │ │              │ │              │
    │ Green ✓       │ │ Gold border  │ │ Orange border│
    │               │ │ 📉 Fading    │ │ ⚠️ Wilting   │
    │ Stats show    │ │              │ │ Low opacity  │
    │               │ │ Action:      │ │              │
    │ Action:       │ │ Review soon  │ │ Action:      │
    │ None needed   │ │              │ │ Review NOW   │
    └────────┬──────┘ └──────┬───────┘ └──────┬───────┘
             │                │                │
    [2 days pass]   [continues to   [continues to
             │       day 7]          day 14]
             │                │                │
             ▼                ▼                ▼
    ┌───────────────┐ ┌──────────────┐ ┌──────────────┐
    │ Still FRESH   │ │ Enters       │ │ WILTED (0%)  │
    │ (100%)        │ │ WILTING      │ │              │
    │ No change     │ │ (50%)        │ │ Red border   │
    │ unless        │ │              │ │ 🥀 emoji     │
    │ replayed      │ │              │ │ URGENT badge │
    │               │ │              │ │              │
    └────────┬──────┘ └──────┬───────┘ │ Action:      │
             │                │        │ MUST replay  │
             │                │        └──────┬───────┘
             └────────────────┤              │
                              │              │
                    [Player replays stage]
                              │
                              ▼
                    ┌──────────────────┐
                    │ On successful    │
                    │ completion:      │
                    │ lastPlayedTS=now │
                    └────────┬─────────┘
                             │
                    ┌────────▼─────────┐
                    │ Back to FRESH    │
                    │ (100%) cycle     │
                    │ resets           │
                    └──────────────────┘
```

---

## Component Integration Map

```
┌──────────────────────────────────────────────────────────────┐
│                    HOME.TSX (HOME SCREEN)                    │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ Main component: HomeScreen()                           │  │
│  │ - Calls: useProgress() → state, canUnlockStage         │  │
│  │ - Imports: calculateFreshness (NEW)                    │  │
│  └────────────┬─────────────────────────────────────────┘  │
│               │                                               │
│               ├─► PlantGrowth (unchanged)                    │
│               │   Shows level & XP bar                       │
│               │                                               │
│               ├─► statsCard (unchanged)                      │
│               │   Shows Water Drops & Plots Mastered        │
│               │                                               │
│               ├─► importButton (unchanged)                   │
│               │   Plant New Seeds                            │
│               │                                               │
│               └─► Object.entries(materials).map()            │
│                   │                                          │
│                   └─► MaterialSection for each plot          │
│                       │                                      │
│                       └─► StageCard × 3 (Remember,           │
│                           Understand, Apply)                │
│                                                               │
│   ┌──────────────────────────────────────────────────────┐  │
│   │ StageCard PROPS (NEW):                               │  │
│   ├──────────────────────────────────────────────────────┤  │
│   │ freshness: number (0-100)                            │  │
│   │  ↓                                                    │  │
│   │  Passed from: calculateFreshness(                    │  │
│   │    material.stageResults?.remember?.lastPlayedTS     │  │
│   │  )                                                    │  │
│   └──────────────────────────────────────────────────────┘  │
│                       │                                      │
│                       ▼                                      │
│   ┌──────────────────────────────────────────────────────┐  │
│   │ StageCard INTERNAL LOGIC (NEW):                      │  │
│   ├──────────────────────────────────────────────────────┤  │
│   │ const isFullyWilted = isCompleted && (f === 0)      │  │
│   │ const isWilting = isCompleted && (f <= 50 && f > 0) │  │
│   │ const isDrooping = isCompleted && (f === 75)        │  │
│   │                                                      │  │
│   │ Applies styles:                                      │  │
│   │ - stageCardWilted (f=0)                              │  │
│   │ - stageCardWilting (f=50)                            │  │
│   │ - stageCardDrooping (f=75)                           │  │
│   │ - stageCardCompleted (f=100)                         │  │
│   └──────────────────────────────────────────────────────┘  │
│                       │                                      │
│                       ▼                                      │
│   ┌──────────────────────────────────────────────────────┐  │
│   │ RENDERING (CONDITIONAL):                             │  │
│   ├──────────────────────────────────────────────────────┤  │
│   │ FRESH (100%):                                        │  │
│   │  Emoji: 🌾 | Text: "92% Accuracy · x5 Combo"        │  │
│   │            | Checkmark: ✓                            │  │
│   │                                                      │  │
│   │ DROOPING (75%):                                      │  │
│   │  Emoji: 🌾 | Text: "📉 Fading (review soon)"        │  │
│   │            | Border: Gold (#FFD54F)                  │  │
│   │                                                      │  │
│   │ WILTING (50%):                                       │  │
│   │  Emoji: 🌾 | Text: "⚠️ Knowledge wilting"           │  │
│   │            | Border: Orange (#FFB74D)                │  │
│   │            | Opacity: 0.85                           │  │
│   │                                                      │  │
│   │ WILTED (0%):                                         │  │
│   │  Emoji: 🥀 | Badge: "Needs Watering!"               │  │
│   │            |        "Replay to restore"              │  │
│   │            | Border: Red (#EF5350)                   │  │
│   │            | Opacity: 0.75                           │  │
│   └──────────────────────────────────────────────────────┘  │
│                                                               │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│              PROGRESSCONTEXT.TSX                             │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ Export: calculateFreshness(lastPlayedTS?: number): number
│ │                                                          │ │
│ │ Logic:                                                  │ │
│ │  if !lastPlayedTS → return 100 (fresh)                 │ │
│ │  elapsedDays = (now - lastPlayedTS) / MS_PER_DAY        │ │
│ │  if elapsedDays <= 2 → 100 (fresh)                     │ │
│ │  if elapsedDays <= 6 → 75 (drooping)                   │ │
│ │  if elapsedDays <= 13 → 50 (wilting)                   │ │
│ │  else → 0 (wilted)                                      │ │
│ └──────────────────────────────────────────────────────────┘ │
│                       │                                       │
│                       ▼                                       │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ StageResult interface (NEW field):                       │ │
│ │ {                                                        │ │
│ │   accuracy: 0-1                                          │ │
│ │   maxCombo: number                                       │ │
│ │   attempts: number                                       │ │
│ │   timestamp: number                                      │ │
│ │   lastPlayedTimestamp?: number (NEW)                     │ │
│ │ }                                                        │ │
│ └──────────────────────────────────────────────────────────┘ │
│                       │                                       │
│                       ▼                                       │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ completeStage() function (MODIFIED):                     │ │
│ │                                                          │ │
│ │ Old: [stageKey]: { accuracy, maxCombo, ... }            │ │
│ │ New: [stageKey]: {                                       │ │
│ │   accuracy, maxCombo, attempts, timestamp,              │ │
│ │   lastPlayedTS: unlocked                                │ │
│ │            ? Date.now()   (NEW on success)              │ │
│ │            : existing || Date.now()                     │ │
│ │ }                                                        │ │
│ │                                                          │ │
│ │ When called:                                             │ │
│ │ - User completes Remember/Understand/Apply             │ │
│ │ - completeStage() called w/ accuracy, maxCombo           │ │
│ │ - If unlocked (passed mastery gate):                    │ │
│ │   → Update lastPlayedTS to now (refresh clock)          │ │
│ │ - Else:                                                  │ │
│ │   → Keep existing lastPlayedTS                          │ │
│ │ - Sync to Firestore                                      │ │
│ └──────────────────────────────────────────────────────────┘ │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

---

## Data Persistence Flow

```
┌────────────────────────────────────────┐
│ User completes stage successfully      │
│ (accuracy >= 80% && maxCombo >= 3)    │
└─────────────┬──────────────────────────┘
              │
              ▼
      ┌──────────────────┐
      │ completeStage()  │
      │   unlocked=true  │
      └─────────┬────────┘
                │
                ▼
    ┌─────────────────────────┐
    │ Set lastPlayedTS =      │
    │ Date.now()              │
    │ (e.g., 1708390000000)   │
    └─────────────┬───────────┘
                  │
                  ▼
    ┌─────────────────────────────────┐
    │ Update React State              │
    │ setState({                      │
    │   materials: {                  │
    │     [id]: {                     │
    │       stageResults: {           │
    │         remember: {             │
    │           timestamp: 1708390... │
    │           lastPlayedTS:1708390..│
    │         }                       │
    │       }                         │
    │     }                           │
    │   }                             │
    │ })                              │
    └─────────────┬───────────────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
        ▼                   ▼
  ┌──────────────┐  ┌──────────────────┐
  │ AsyncStorage │  │ Firestore        │
  │ .setItem(    │  │ .updateDoc(      │
  │   data       │  │   stageResults   │
  │ )            │  │ )                │
  └──────────────┘  └──────────────────┘
        │                   │
        └─────────┬─────────┘
                  │
          [Days pass...]
                  │
        ┌─────────▼──────────┐
        │ App reopens        │
        │ (user log in)      │
        └─────────┬──────────┘
                  │
        ┌─────────▼──────────────┐
        │ Load Firestore or      │
        │ AsyncStorage           │
        └─────────┬──────────────┘
                  │
        ┌─────────▼───────────────┐
        │ Hydrate React state     │
        │ with lastPlayedTS       │
        │ (original timestamp)    │
        └─────────┬───────────────┘
                  │
        ┌─────────▼────────────────┐
        │ calculateFreshness()     │
        │ compares (now - time)    │
        │ returns freshness %      │
        └─────────┬────────────────┘
                  │
        ┌─────────▼──────────────┐
        │ StageCard renders      │
        │ with freshness value   │
        │ → visual feedback      │
        └────────────────────────┘
```

---

## Decision Tree: Should This Card Show Wilting?

```
START: Is this stage in stagesCompleted?
  │
  NO ├─► Show as LOCKED, RETRY, or PLAYABLE
  │   └─► No wilting (not completed yet)
  │
  YES ├─► Get lastPlayedTimestamp from stageResults
      │
      ├─► Call calculateFreshness(timestamp)
      │
      └─► Check returned value:
          │
          ├─ freshness === 100?
          │  └─► FRESH state (green, ✓, stats)
          │
          ├─ freshness === 75?
          │  └─► DROOPING state (gold, 📉)
          │
          ├─ freshness === 50?
          │  └─► WILTING state (orange, ⚠️, low opacity)
          │
          └─ freshness === 0?
             └─► WILTED state (red, 🥀, badge, URGENT)
```

---

## Key Numbers

| Metric | Value | Notes |
|--------|-------|-------|
| Fresh window | 0-2 days | Two days to do first review |
| Drooping window | 3-6 days | Three days until gentle warning |
| Wilting window | 7-13 days | One week before urgent state |
| Wilted threshold | 14+ days | Two weeks = knowledge lost |
| Fresh opacity | 1.0 | Full visibility |
| Wilting opacity | 0.85 | Slightly transparent |
| Wilted opacity | 0.75 | More transparent, clearly faded |
| Timestamp precision | milliseconds | 1000ms = 1 second |
| Days calculation | 86400000 ms | (1000 * 60 * 60 * 24) |

---

## State Transitions Summary

```
┌──────────────────────────────────────────────────┐
│ Initial: No stage data                           │
└────────────┬─────────────────────────────────────┘
             │
             ▼
      Player completes
             │
             ▼
    ┌─────────────────┐
    │  FRESH (100%)   │ ◄─────────────────────┐
    │  Green / ✓      │                      │
    │  Days 0-2       │                      │
    └────────┬────────┘                      │
             │                               │
      [2 days pass]                    Player replays
             │                               │
             ▼                               │
    ┌─────────────────┐                      │
    │ DROOPING (75%)  │                      │
    │ Gold / 📉       │                      │
    │ Days 3-6        │                      │
    └────────┬────────┘                      │
             │                               │
      [4 days pass]                          │
             │                               │
             ▼                               │
    ┌─────────────────┐                      │
    │ WILTING (50%)   │                      │
    │ Orange / ⚠️     │                      │
    │ Days 7-13       │                      │
    └────────┬────────┘                      │
             │                               │
      [1+ days pass]                         │
             │                               │
             ▼                               │
    ┌─────────────────┐                      │
    │ WILTED (0%)     │                      │
    │ Red / 🥀        │                      │
    │ Days 14+        │                      │
    └────────┬────────┘                      │
             │                               │
             └───────────────────────────────┘
                   Player Replays
```

